apiVersion: apps/v1
kind: Deployment
metadata:
  name: selection-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: selection-service
  template:
    metadata:
      labels:
        app: selection-service
    spec:
      containers:
        - name: selection-service
          image: your-registry/selection-service:latest
          ports:
            - containerPort: 8080
          env:
            - name: POSTGRES_DSN
              valueFrom:
                secretKeyRef:
                  name: postgres-secret
                  key: dsn
            - name: REDIS_ADDR
              value: "redis:6379"
            - name: KAFKA_BROKER
              value: "kafka:9092"
